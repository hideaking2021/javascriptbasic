'use strict';

// 変数 let

let price = 500;

//price = price + 100;
price += 100;

//price = price * 2;
price *= 2;

// price = price + 1;
// price += 1;
price++;

// price = price - 1;
// price -= 1;
price--;

console.log(price);
